[project]
name = "globalbrain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pekuhn", email = "peterkuhn92@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
]

[project.scripts]
globalbrain = "globalbrain:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
